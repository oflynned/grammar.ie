---
interface Props {
    tag: string;
}

const {tag} = Astro.props
const label = tag
    .replace(/-/g, ' ')
    .replace(/\b\w/g, (letter) => letter.toUpperCase());
---

<span class="tag">
    {label}
</span>
